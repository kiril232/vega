apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: vega
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: kiril232/vega-payment-service:cd02ec69ce933ce9225923596f41bb72462e0ed2
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: vega-config
          env:
            - name: ConnectionStrings__Default
              value: "Host=postgres;Port=5432;Database=vega;Username=vega;Password=vega;"
            - name: Jwt__Key
              valueFrom:
                secretKeyRef:
                  name: vega-secret
                  key: jwt-key
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"

---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: vega
spec:
  selector:
    app: payment-service
  ports:
    - port: 8080
      targetPort: 8080