apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: vega
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: kiril232/vega-user-service:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: vega-config
          env:
            - name: ConnectionStrings__Default
              value: "Host=postgres;Port=5432;Database=vega;Username=vega;Password=vega;"
            - name: Jwt__Key
              valueFrom:
                secretKeyRef:
                  name: vega-secret
                  key: jwt-key
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"

---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: vega
spec:
  selector:
    app: user-service
  ports:
    - port: 8080
      targetPort: 8080